// priority: 0

// Hide items
JEIEvents.hideItems(event => {
  event.hide([

    // Wrenches
    '#ae2:quartz_wrench',
    'enderio:yeta_wrench',
    'etcetera:wrench',

  ]);
});

// Add items
JEIEvents.addItems(event => {
  event.add([
  ]);
});